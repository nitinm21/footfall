# @footfall/target

The test rig: a small Next.js docs site with failure modes deliberately baked in —
a client-side-rendered-only page (empty shell), no `/llms.txt`, a moved URL (old path
404s), an auth-walled route, and normal static docs pages.

Built in Phase 1 and reused by every later phase's integration tests. Placeholder for now.
