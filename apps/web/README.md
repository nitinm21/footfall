# @footfall/web

Dashboard + `/api/ingest` + onboarding + public docs — a single Next.js (App Router)
deploy on Vercel.

**Status:** placeholder. Scaffolded into a real Next.js app across Phases 4–6:

- Phase 4 — `/api/ingest` (edge): token check → zod validation → forward to Tinybird.
- Phase 5 — the Live Dashboard (design contract = the approved mock), Auth.js GitHub
  OAuth, Neon Postgres metadata, per-tenant isolation.
- Phase 6 — self-serve onboarding, install docs / privacy page, digests.
