// @footfall/cli — Phase 6. Published to npm as `footfall` so `npx footfall` stays short.
// `footfall init <token>`: verify token -> detect project -> install @footfall/next ->
// create/codemod middleware.ts + status beacons -> write env. `footfall check`:
// post-deploy round-trip verification that closes the loop by being the traffic.
export {};
