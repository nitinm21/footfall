// aggregate/ — Phase 2.
// One function per dashboard/report module: traffic split, daily rollup, family
// breakdown, top pages by agent demand, failure aggregates, coverage matrix.
// These are THE definitions; the report renders them and Tinybird pipes mirror
// them (a parity test enforces equality).
export {};
