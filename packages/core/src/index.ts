// @footfall/core — the spine. Pure functions only, no I/O.
//
// Phase 0 ships the event contract; later phases fill in the pipeline stages
// re-exported below (all currently placeholders):
//   normalize/  (Phase 2)  detect/     (Phase 2)
//   classify/   (Phase 2)  recommend   (Phase 2)
//   sessionize  (Phase 2)  aggregate/  (Phase 2)

export * from "./schema";
