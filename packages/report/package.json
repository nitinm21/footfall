{
  "name": "@footfall/report",
  "version": "0.0.0",
  "private": true,
  "description": "Renders an AnalysisResult into a self-contained static HTML report (the wedge product).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
